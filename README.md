# kcd-kuala-lumpur.github.io

## Run in local machine

Run

```
docker compose watch
```

locally to check the changes before pushing to remote.

Make sure your docker compose version is `2.22.0 or higher` for that command to work.

To cleanup, run

```
docker compose down
```
